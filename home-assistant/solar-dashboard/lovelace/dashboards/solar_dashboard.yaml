# 332 Three Mile Bush Road — AI archviz render (v15 backgrounds)
# cache-bust 2026-06-02: v15 — master pixels + weather grades; optional sky tidy
views:
  - title: Solar
    theme: default
    type: panel
    panel: true
    cards:
      - type: picture-elements
        card_mod:
          style: |
            :host {
              display: flex !important;
              align-items: center !important;
              justify-content: center !important;
              height: 100vh !important;
              overflow: hidden !important;
            }
            ha-card {
              width: min(100vw, calc(100vh * 16 / 9)) !important;
              max-width: 100vw !important;
              aspect-ratio: 16 / 9 !important;
              height: auto !important;
              margin: 0 auto !important;
              border: none !important;
              box-shadow: none !important;
              overflow: hidden !important;
              background: #000 !important;
            }
            #root picture-elements,
            picture-elements {
              overflow: hidden !important;
              max-width: 100% !important;
            }
            ha-card img {
              width: 100% !important;
              height: 100% !important;
              object-fit: contain !important;
              object-position: center center !important;
              display: block !important;
            }
        entity: sensor.solar_dashboard_background
        state_image:
          clear: /local/solar-dashboard/backgrounds/v15/clear.jpg?v=20260602b
          cloudy: /local/solar-dashboard/backgrounds/v15/cloudy.jpg?v=20260602b
          covered: /local/solar-dashboard/backgrounds/v15/covered.jpg?v=20260602b
          very_covered: /local/solar-dashboard/backgrounds/v15/very_covered.jpg?v=20260602b
          night: /local/solar-dashboard/backgrounds/v15/night.jpg?v=20260602b
        image: /local/solar-dashboard/backgrounds/v15/clear.jpg?v=20260602b
        elements:
          - type: custom:button-card
            name: Contact Energy TOU
            layout: grid
            show_icon: true
            icon: mdi:flash
            show_state: false
            styles:
              grid:
                - grid-template-areas: '"i n" "rows rows"'
                - grid-template-columns: 20px 1fr
              card:
                - background: rgba(8, 22, 48, 0.86)
                - border-radius: 12px
                - padding: 8px 10px
                - width: 148px
                - box-shadow: 0 4px 14px rgba(0,0,0,0.4)
              icon:
                - color: "#f5c842"
                - width: 18px
              name:
                - color: rgba(255,255,255,0.9)
                - font-size: 10px
                - font-weight: 700
              custom_fields:
                rows:
                  - grid-area: rows
            custom_fields:
              rows: |
                [[[
                  const peak = states['sensor.contact_tariff_peak_display']?.state ?? '33.5 c/kWh';
                  const off = states['sensor.contact_tariff_offpeak_display']?.state ?? '16.5 c/kWh';
                  return `<motion.div style="font-size:10px;color:#fff;line-height:1.65;margin-top:4px;">
                    <motion.div style="display:flex;justify-content:space-between;gap:6px;"><span style="color:#ffb74d;">Peak 7am–9pm</span><span>${peak}</span></motion.div>
                    <motion.div style="display:flex;justify-content:space-between;gap:6px;"><span style="color:#66bb6a;">Off-peak</span><span>${off}</span></motion.div>
                    <motion.div style="font-size:9px;opacity:0.75;margin-top:4px;">Northpower network</motion.div>
                  </motion.div>`.replace(/motion\./g,'');
                ]]]
            card_mod:
              style: |
                ha-card {
                  background: rgba(8, 22, 48, 0.86) !important;
                  border-radius: 12px !important;
                  padding: 8px 10px !important;
                  width: 148px !important;
                  max-width: 148px !important;
                  box-shadow: 0 4px 14px rgba(0,0,0,0.4) !important;
                  border: none !important;
                }
            style:
              top: 5%
              left: 5%

          - type: custom:button-card
            entity: sensor.solar_forecast_today
            name: Solar forecast
            layout: grid
            show_icon: true
            icon: mdi:solar-power
            show_state: false
            styles:
              grid:
                - grid-template-areas: '"i n" "rows rows"'
                - grid-template-columns: 20px 1fr
              card:
                - background: rgba(8, 22, 48, 0.86)
                - border-radius: 12px
                - padding: 8px 10px
                - width: 148px
                - box-shadow: 0 4px 14px rgba(0,0,0,0.4)
              icon:
                - color: "#f5c842"
                - width: 18px
              name:
                - color: rgba(255,255,255,0.9)
                - font-size: 10px
                - font-weight: 700
              custom_fields:
                rows:
                  - grid-area: rows
            custom_fields:
              rows: |
                [[[
                  const today = states['sensor.solar_forecast_today']?.state ?? '—';
                  const tomorrow = states['sensor.solar_forecast_tomorrow']?.state ?? '—';
                  return `<motion.div style="font-size:10px;color:#fff;line-height:1.65;margin-top:4px;">
                    <motion.div style="display:flex;justify-content:space-between;gap:6px;"><span style="color:#ffb74d;">Today</span><span>${today} kWh</span></motion.div>
                    <motion.div style="display:flex;justify-content:space-between;gap:6px;"><span style="color:#66bb6a;">Tomorrow</span><span>${tomorrow} kWh</span></motion.div>
                  </motion.div>`.replace(/motion\./g,'');
                ]]]
            card_mod:
              style: |
                :host {
                  right: 5% !important;
                  left: auto !important;
                  width: 148px !important;
                  max-width: 148px !important;
                }
                ha-card {
                  background: rgba(8, 22, 48, 0.86) !important;
                  border-radius: 12px !important;
                  padding: 8px 10px !important;
                  width: 148px !important;
                  max-width: 148px !important;
                  box-shadow: 0 4px 14px rgba(0,0,0,0.4) !important;
                  border: none !important;
                }
            style:
              top: 5%
              right: 5%
              left: auto
              width: 148px

          - type: custom:button-card
            entity: weather.forecast_home
            name: Weather
            layout: grid
            show_icon: true
            icon: mdi:weather-partly-cloudy
            show_state: false
            styles:
              grid:
                - grid-template-areas: '"i n" "rows rows"'
                - grid-template-columns: 20px 1fr
              card:
                - background: rgba(8, 22, 48, 0.86)
                - border-radius: 12px
                - padding: 8px 10px
                - width: 148px
                - box-shadow: 0 4px 14px rgba(0,0,0,0.4)
              icon:
                - color: "#f5c842"
                - width: 18px
              name:
                - color: rgba(255,255,255,0.9)
                - font-size: 10px
                - font-weight: 700
              custom_fields:
                rows:
                  - grid-area: rows
            custom_fields:
              rows: |
                [[[
                  const pick = (id) => states[id];
                  const w = pick('weather.forecast_home') || pick(states['input_text.solar_entity_weather']?.state) || pick('weather.home');
                  let fc = w?.attributes?.forecast ?? w?.attributes?.forecast_daily ?? [];
                  if (!fc.length && Array.isArray(w?.attributes?.forecast_hourly)) {
                    const seen = new Set();
                    fc = w.attributes.forecast_hourly.filter((h) => {
                      const d = (h.datetime || '').slice(0, 10);
                      if (!d || seen.has(d)) return false;
                      seen.add(d);
                      return true;
                    }).slice(0, 2);
                  }
                  const today = fc[0] || (w ? { condition: w.state, temperature: w.attributes?.temperature } : null);
                  const tomorrow = fc[1] || null;
                  const row = (label, day) => {
                    if (!day) return `<motion.div style="display:flex;justify-content:space-between;gap:6px;"><span>${label}</span><span>—</span></motion.div>`;
                    const cond = (day.condition || day.state || '—').replace(/_/g, ' ');
                    const temp = day.temperature != null ? Math.round(day.temperature) + '°' : '—';
                    return `<motion.div style="display:flex;justify-content:space-between;gap:6px;"><span style="color:#90caf9;">${label}</span><span style="text-transform:capitalize;">${cond} ${temp}</span></motion.div>`;
                  };
                  return `<motion.div style="font-size:10px;color:#fff;line-height:1.65;margin-top:4px;">${row('Today', today)}${row('Tomorrow', tomorrow)}</motion.div>`.replace(/motion\./g,'');
                ]]]
            card_mod:
              style: |
                :host {
                  right: 5% !important;
                  left: auto !important;
                  width: 148px !important;
                  max-width: 148px !important;
                }
                ha-card {
                  background: rgba(8, 22, 48, 0.86) !important;
                  border-radius: 12px !important;
                  padding: 8px 10px !important;
                  width: 148px !important;
                  max-width: 148px !important;
                  box-shadow: 0 4px 14px rgba(0,0,0,0.4) !important;
                  border: none !important;
                }
            style:
              top: 16%
              right: 5%
              left: auto
              width: 148px

          - type: custom:button-card
            entity: sensor.grid_import_power
            name: Grid use
            icon: mdi:transmission-tower
            show_icon: true
            show_name: true
            show_state: true
            state_display: |
              [[[
                return (entity.state ?? '0') + ' W';
              ]]]
            styles:
              card:
                - background: rgba(8, 22, 48, 0.86)
                - border-radius: 12px
                - padding: 8px 10px
                - width: 154px
                - box-shadow: 0 4px 14px rgba(0,0,0,0.4)
              icon:
                - color: "#f5c842"
                - width: 22px
              name:
                - color: rgba(255,255,255,0.9)
                - font-size: 10px
                - font-weight: 700
              state:
                - color: "#fff"
                - font-size: 16px
                - font-weight: 700
            style:
              top: 15%
              left: 60%

          - type: custom:button-card
            entity: sensor.home_consumption_power
            name: Home use
            icon: mdi:home-lightning-bolt
            show_icon: true
            show_name: true
            show_state: true
            state_display: |
              [[[
                return (entity.state ?? '0') + ' W';
              ]]]
            styles:
              card:
                - background: rgba(8, 22, 48, 0.9)
                - border-radius: 14px
                - padding: 12px 16px
                - width: 172px
                - box-shadow: 0 6px 18px rgba(0,0,0,0.45)
              icon:
                - color: "#f5c842"
                - width: 30px
              name:
                - color: rgba(255,255,255,0.85)
                - font-size: 12px
                - font-weight: 700
              state:
                - color: "#fff"
                - font-size: 20px
                - font-weight: 700
            style:
              top: 42%
              left: 42%

          - type: custom:button-card
            entity: sensor.solar_battery_soc
            name: Battery
            layout: grid
            icon: mdi:battery-high
            show_icon: true
            show_state: false
            styles:
              card:
                - background: rgba(20, 28, 40, 0.75)
                - border-radius: 12px 0 0 12px
                - border: none
                - border-left: 1px solid rgba(255, 255, 255, 0.08)
                - padding: 8px 10px
                - width: 175px
                - box-shadow: inset 6px 0 12px rgba(0, 0, 0, 0.35), 2px 4px 10px rgba(0, 0, 0, 0.22)
              grid:
                - grid-template-areas: '"i n n" "batt batt batt"'
                - grid-template-columns: 22px 1fr 1fr
              icon:
                - color: "#f5c842"
                - width: 24px
              name:
                - color: rgba(255,255,255,0.9)
                - font-size: 11px
                - font-weight: 700
              custom_fields:
                batt:
                  - grid-area: batt
            custom_fields:
              batt: |
                [[[
                  const soc = Number(entity.state) || 0;
                  const h = Math.max(4, Math.round(soc * 0.72));
                  const pwr = Math.abs(parseInt(states['sensor.solar_battery_power']?.state) || 0);
                  return `<motion.div style="display:flex;gap:8px;margin-top:6px;">
                    <motion.div style="flex:1;font-size:10px;color:rgba(255,255,255,0.9);line-height:1.5;">
                      <motion.div style="display:flex;justify-content:space-between;"><span style="color:#b39ddb;">Power</span><span style="color:#ff5252;font-weight:700;font-size:12px;">${pwr} W</span></motion.div>
                      <motion.div style="display:flex;justify-content:space-between;"><span>SOC</span><span style="color:#ffb74d;font-weight:700;font-size:12px;">${soc}%</span></motion.div>
                      <motion.div style="display:flex;justify-content:space-between;"><span>Voltage</span><span style="font-size:11px;">${states['sensor.solar_battery_voltage']?.state ?? '—'} V</span></motion.div>
                    </motion.div>
                    <motion.div style="width:28px;border:2px solid rgba(255,255,255,0.3);border-radius:5px;padding:2px;display:flex;flex-direction:column;justify-content:flex-end;height:68px;">
                      <motion.div style="height:${h}px;background:linear-gradient(0deg,#2e7d32,#b6ff00);border-radius:3px;"></motion.div>
                    </motion.div>
                  </motion.div>`.replace(/motion\./g,'');
                ]]]
            card_mod:
              style: |
                ha-card {
                  background: rgba(20, 28, 40, 0.75) !important;
                  border: none !important;
                  border-left: 1px solid rgba(255, 255, 255, 0.08) !important;
                  border-radius: 12px 0 0 12px !important;
                  position: relative;
                  overflow: visible;
                  transform-origin: center right;
                  transform: perspective(900px) rotateY(-12deg) rotateX(2deg) skewY(0.8deg) rotateZ(-0.2deg);
                  box-shadow:
                    inset 10px 0 16px rgba(0, 0, 0, 0.28),
                    inset -1px 0 0 rgba(255, 255, 255, 0.16),
                    -12px 10px 18px rgba(0, 0, 0, 0.35) !important;
                  backdrop-filter: blur(4px);
                }
                ha-card::after {
                  content: "";
                  position: absolute;
                  top: 10px;
                  bottom: 10px;
                  right: 0;
                  width: 2px;
                  border-radius: 2px;
                  background: linear-gradient(
                    180deg,
                    rgba(255, 255, 255, 0.34),
                    rgba(255, 255, 255, 0.06)
                  );
                  opacity: 0.75;
                  pointer-events: none;
                }
            style:
              top: 44%
              left: 76%
              z-index: 12

          - type: custom:button-card
            name: Garage
            layout: grid
            show_icon: true
            icon: mdi:garage
            show_state: false
            styles:
              grid:
                - grid-template-areas: '"i n" "ev ev"'
                - grid-template-columns: 18px 1fr
              card:
                - background: rgba(20, 28, 40, 0.75)
                - border-radius: 12px 0 0 12px
                - border: none
                - border-left: 1px solid rgba(255, 255, 255, 0.08)
                - padding: 6px 9px
                - width: 175px
                - box-shadow: inset 6px 0 12px rgba(0, 0, 0, 0.35), 2px 4px 10px rgba(0, 0, 0, 0.22)
              icon:
                - color: "#f5c842"
                - width: 16px
              name:
                - color: rgba(255,255,255,0.9)
                - font-size: 10px
                - font-weight: 700
              custom_fields:
                ev:
                  - grid-area: ev
            custom_fields:
              ev: |
                [[[
                  const row = (name, soc, pwr, st) => {
                    const charging = ['charging','connected'].includes((st||'').toLowerCase());
                    const pColor = charging ? '#66bb6a' : '#fff';
                    return `<motion.div style="font-size:9px;line-height:1.4;margin-top:3px;padding-top:3px;border-top:1px solid rgba(255,255,255,0.12);">
                      <motion.div style="font-weight:700;color:#f5c842;margin-bottom:1px;font-size:9px;">${name}</motion.div>
                      <motion.div style="display:flex;justify-content:space-between;color:rgba(255,255,255,0.85);"><span>Batt</span><span>${soc}%</span></motion.div>
                      <motion.div style="display:flex;justify-content:space-between;color:rgba(255,255,255,0.85);"><span>Chg</span><span style="color:${pColor};font-weight:600;">${pwr} kW</span></motion.div>
                      <motion.div style="display:flex;justify-content:space-between;color:rgba(255,255,255,0.65);"><span>St</span><span style="text-transform:capitalize;">${st}</span></motion.div>
                    </motion.div>`;
                  };
                  const sSoc = states['sensor.garage_model_s_battery']?.state ?? '—';
                  const sPwr = states['sensor.garage_model_s_charger_power']?.state ?? '0';
                  const sSt = states['sensor.garage_model_s_charging']?.state ?? '—';
                  const xSoc = states['sensor.garage_model_x_battery']?.state ?? '—';
                  const xPwr = states['sensor.garage_model_x_charger_power']?.state ?? '0';
                  const xSt = states['sensor.garage_model_x_charging']?.state ?? '—';
                  return row('Model S', sSoc, sPwr, sSt) + row('Model X', xSoc, xPwr, xSt);
                ]]]
            card_mod:
              style: |
                ha-card {
                  background: rgba(20, 28, 40, 0.75) !important;
                  border: none !important;
                  border-left: 1px solid rgba(255, 255, 255, 0.08) !important;
                  border-radius: 12px 0 0 12px !important;
                  position: relative;
                  overflow: visible;
                  transform-origin: center right;
                  transform: perspective(900px) rotateY(-12deg) rotateX(2deg) skewY(0.8deg) rotateZ(-0.4deg);
                  box-shadow:
                    inset 10px 0 16px rgba(0, 0, 0, 0.28),
                    inset -1px 0 0 rgba(255, 255, 255, 0.16),
                    -12px 10px 18px rgba(0, 0, 0, 0.35) !important;
                  backdrop-filter: blur(4px);
                }
                ha-card::after {
                  content: "";
                  position: absolute;
                  top: 10px;
                  bottom: 10px;
                  right: 0;
                  width: 2px;
                  border-radius: 2px;
                  background: linear-gradient(
                    180deg,
                    rgba(255, 255, 255, 0.34),
                    rgba(255, 255, 255, 0.06)
                  );
                  opacity: 0.75;
                  pointer-events: none;
                }
            style:
              top: 56%
              left: 76%
              z-index: 12

          - type: custom:button-card
            name: Grid details
            layout: grid
            show_icon: false
            show_state: false
            styles:
              grid:
                - grid-template-areas: '"n n" "grid grid"'
              card:
                - background: rgba(8, 22, 48, 0.86)
                - border-radius: 12px
                - padding: 8px 10px
                - width: 148px
                - box-shadow: 0 4px 14px rgba(0,0,0,0.4)
              name:
                - color: rgba(255,255,255,0.9)
                - font-size: 10px
                - font-weight: 700
              custom_fields:
                grid:
                  - grid-area: grid
            custom_fields:
              grid: |
                [[[
                  const v = states['sensor.grid_voltage']?.state ?? '—';
                  const t = states['sensor.inverter_temperature']?.state ?? '—';
                  const c = states['sensor.battery_cycle_count']?.state ?? '—';
                  const row = (label, val) =>
                    `<motion.div style="font-size:10px;color:rgba(255,255,255,0.85);line-height:1.65;display:flex;justify-content:space-between;gap:8px;">
                      <span>${label}</span><span style="font-weight:600;">${val}</span>
                    </motion.div>`;
                  return row('Voltage', v + ' V') + row('Temp', t + ' °C') + row('Cycles', c);
                ]]]
            style:
              top: 27%
              left: 60%

          - type: custom:button-card
            entity: sensor.solar_ground_power
            name: Solar array
            icon: mdi:solar-panel
            show_icon: true
            show_name: true
            show_state: true
            state_display: |
              [[[
                return (entity.state ?? '0') + ' W';
              ]]]
            styles:
              card:
                - background: rgba(8, 22, 48, 0.92)
                - border-radius: 16px
                - padding: 12px 10px
                - width: 155px
                - max-width: 155px
                - box-shadow: 0 6px 18px rgba(0,0,0,0.5)
              icon:
                - color: "#f5c842"
                - width: 36px
              name:
                - color: rgba(255,255,255,0.9)
                - font-size: 12px
                - font-weight: 700
              state:
                - color: "#fff"
                - font-size: 22px
                - font-weight: 700
            style:
              bottom: 5%
              left: 5%

          - type: custom:button-card
            entity: sensor.solar_pv_total_today
            name: PV total
            icon: mdi:solar-power-variant
            show_icon: true
            show_name: true
            show_state: true
            state_display: |
              [[[
                return (entity.state ?? '—') + ' kWh';
              ]]]
            styles:
              card:
                - background: rgba(8, 22, 48, 0.92)
                - border-radius: 16px
                - padding: 12px 10px
                - width: 155px
                - max-width: 155px
                - box-shadow: 0 6px 18px rgba(0,0,0,0.5)
              icon:
                - color: "#f5c842"
                - width: 36px
              name:
                - color: rgba(255,255,255,0.9)
                - font-size: 12px
                - font-weight: 700
              state:
                - color: "#fff"
                - font-size: 22px
                - font-weight: 700
            style:
              bottom: 5%
              left: 15%

          - type: custom:button-card
            entity: sensor.solar_production_today
            name: Solar today
            icon: mdi:white-balance-sunny
            show_icon: true
            show_name: true
            show_state: true
            state_display: |
              [[[
                return (entity.state ?? '—') + ' kWh';
              ]]]
            styles:
              card:
                - background: rgba(8, 22, 48, 0.92)
                - border-radius: 16px
                - padding: 12px 10px
                - width: 155px
                - max-width: 155px
                - box-shadow: 0 6px 18px rgba(0,0,0,0.5)
              icon:
                - color: "#f5c842"
                - width: 36px
              name:
                - color: rgba(255,255,255,0.9)
                - font-size: 12px
                - font-weight: 700
              state:
                - color: "#fff"
                - font-size: 22px
                - font-weight: 700
            style:
              bottom: 5%
              left: 25%

          - type: custom:button-card
            entity: sensor.solar_production_month
            name: Solar month
            icon: mdi:calendar-month
            show_icon: true
            show_name: true
            show_state: true
            state_display: |
              [[[
                return (entity.state ?? '—') + ' kWh';
              ]]]
            styles:
              card:
                - background: rgba(8, 22, 48, 0.92)
                - border-radius: 16px
                - padding: 12px 10px
                - width: 155px
                - max-width: 155px
                - box-shadow: 0 6px 18px rgba(0,0,0,0.5)
              icon:
                - color: "#f5c842"
                - width: 36px
              name:
                - color: rgba(255,255,255,0.9)
                - font-size: 12px
                - font-weight: 700
              state:
                - color: "#fff"
                - font-size: 22px
                - font-weight: 700
            style:
              bottom: 5%
              left: 35%

          - type: custom:button-card
            entity: sensor.solar_production_year
            name: Solar year
            icon: mdi:chart-bar
            show_icon: true
            show_name: true
            show_state: true
            state_display: |
              [[[
                return (entity.state ?? '—') + ' kWh';
              ]]]
            styles:
              card:
                - background: rgba(8, 22, 48, 0.92)
                - border-radius: 16px
                - padding: 12px 10px
                - width: 155px
                - max-width: 155px
                - box-shadow: 0 6px 18px rgba(0,0,0,0.5)
              icon:
                - color: "#f5c842"
                - width: 36px
              name:
                - color: rgba(255,255,255,0.9)
                - font-size: 12px
                - font-weight: 700
              state:
                - color: "#fff"
                - font-size: 22px
                - font-weight: 700
            style:
              bottom: 5%
              left: 45%
